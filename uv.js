const fs = require("fs");
// Very naive way to find UV bounds if we don't want to parse GLB properly.
// Better to just use a small express server to serve a test image, or edit the component to render a grid!
